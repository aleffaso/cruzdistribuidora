<!--Header-->
<%- include("../partials/header.ejs") %>
<!--Navbar-->
<%- include("../partials/navbar_pages.ejs") %>

<main id="mainProduct">

  <section class="breadcrumbs">
    <div class="container">

      <div class="d-flex justify-content-between align-items-center" style="margin-top: 60px;">
        <h2>Catálogo de produtos</h2>
        <ol>
          <li><a href="/">Início</a></li>
          <li>produtos</li>
        </ol>
      </div>

    </div>

  </section>

  <section id="products" class="portfolio inner-page">
    <div class="container" data-aos="fade-up">
        
      <!-- <div class="row" data-aos="fade-up" data-aos-delay="100">
          <div class="col-lg-12 d-flex justify-content-center">
              <ul id="portfolio-flters">
                  <li data-filter="*" class="filter-active">Todos</li>
                  <% suppliers.forEach(supplier => {%>
                    <li data-filter=".filter-<%= supplier.title %>"><%= supplier.title %></li>
                  <% }) %>
              </ul>
          </div>
      </div>

      <div class="row portfolio-container" data-aos="fade-up" data-aos-delay="200">
        <% products.forEach(products => {%>
          <div class="col-md-6 mt-0  portfolio-item filter-<%= products.supplier.title %>">
            <div class="icon-box-portfolio" style="background: #f0f4f8;">
              <i><img src="<%= products.picture %>"></i>
              <h4><%= products.title %></h4>
              <p>Fornecedor: <%= products.supplier.title %></p>
            </div>
          </div>
        <% }) %>  
      </div> -->

      <div class="row" data-aos="fade-up" data-aos-delay="200">
        <div class="col d-flex justify-content-center text-center">
          <input id="listSearch" type="text" placeholder="Buscar..." class="form-control">
          <div class="btn btn-secondary ms-1" style="color: #fff; background-color: #35a5ed; border-color:#35a5ed;">
            <i class="bi bi-search"></i>
          </div>
        </div>
      </div>

      <div class="mt-4" data-aos="fade-up" data-aos-delay="200">
        <% products.forEach(products => {%>
          <div id="myList">
            <div id="listItem">
              <div class="icon-box-portfolio mt-3" style="background: #f0f4f8;">
                <div class="row row-align">
                  <div class="col-3 col-md-4 list-image">
                    <i><img src="<%= products.picture %>"></i>
                  </div>
                  <div class="col-8 col-side">
                    <h4><%= products.title %></h4>
                    <p>
                      <strong>Fornecedor:</strong> <%= products.supplier.title %>
                      <br><strong>Código:</strong> <%= products.code %>
                    </p>
                  </div>
                </div>                
              </div>
            </div>
          </div>
        <% }) %>  
      </div>

    </div>
  </section>

  </main>

<!--Footer -->
<%- include("../sections/10_footer.ejs") %>
<%- include("../partials/footer.ejs") %>